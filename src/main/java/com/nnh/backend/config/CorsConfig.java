package com.nnh.backend.config;

/**
 * CORS is now configured inside {@link com.nnh.backend.security.SecurityConfig}
 * via the {@code CorsConfigurationSource} bean, which Spring Security picks up
 * automatically.  This file is intentionally empty — kept as a placeholder so
 * the package stays coherent.
 */
public class CorsConfig {
}
